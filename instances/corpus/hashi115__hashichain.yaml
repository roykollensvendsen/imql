schema_version: "1.0.0"
instance_kind: extracted

# No scoring/reward code, no weight-setting call site, no constants/config exist in
# this repo. The only content is a marketing/whitepaper README describing a "Probabilistic
# State Machine" L1 that claims to leverage Bittensor's Yuma Consensus. Mechanism is not
# implemented here and not concretely specified — treated as dormant/unknown.
mechanism_status: unknown
mechanism_status_notes: >-
  Repo contains only a whitepaper-style README, a LICENSE, and an image. No validator,
  reward function, weight-setting call, or constants are present, so the operative status
  of any live mechanism cannot be determined from this source.

subnet:
  name: "HashiChain"
  owner_repo: "hashi115__hashichain"
  netuid: null
  implementation_lang: none_docs_only

task:
  summary: >-
    Per the README, miners operate as "Solver Nodes" running distributed AI models inside
    Trusted Execution Environments (TEEs) to simulate potential agent interactions in secure
    sandboxes, and calculate the probability of a successful "match" between private agent
    intents ("Proof of Entanglement"). No concrete submission format, scoring procedure, or
    reward computation is specified in the source.
  submission_format: [other]
  submission_format_other: >-
    Unspecified. README describes "Solver Nodes" computing probabilistic semantic-match
    proofs inside TEEs, but no concrete submission artifact is defined.

scoring_signals:
  - name: undocumented
    metric_kind: other
    metric_kind_other: >-
      Undocumented. README alludes to probabilistic verification of "semantic compatibility"
      and probability of an intent "match", but no scoring metric is defined in source.
    direction: higher_is_better
    weight: null
    normalization: none
    notes: >-
      Placeholder. No scoring signal is implemented or numerically specified in the repo;
      see provenance.unresolved.

documentation:
  status: whitepaper_only
  source_version: null
  primary_docs:
    - "README.md"
  completeness: low

provenance:
  extracted_by: "im-extract / claude-opus-4-8"
  confidence_overall: low
  evidence:
    - claim_field: "/subnet/name"
      source_path: "README.md"
      line_ref: "1"
      quote: "**HashiChain**: The First Public Blockchain Designed for AI Agents"
    - claim_field: "/subnet/implementation_lang"
      source_path: "README.md"
      line_ref: "1-22"
      quote: >-
        Repo contains only README.md, LICENSE, .gitignore, and assets/intro.png; no source
        code files exist (locate.py: "none_docs_only (0 code / 4 total files)").
    - claim_field: "/task/summary"
      source_path: "README.md"
      line_ref: "12-13"
      quote: >-
        Miners operate as sovereign Solver Nodes within the HashiChain network. By running
        distributed AI models inside Trusted Execution Environments (TEEs), these nodes
        simulate potential interactions in secure sandboxes. ... Nodes calculate the
        probability of a successful "match" between private agent intents
    - claim_field: "/task/submission_format"
      source_path: "README.md"
      line_ref: "13"
      quote: >-
        We replace the deterministic verification of code with the probabilistic verification
        of semantic compatibility.
    - claim_field: "/mechanism_status"
      source_path: "README.md"
      line_ref: "9-10"
      quote: >-
        HashiChain bridges this gap by leveraging the Bittensor network as its consensus
        engine and computational substrate. Yuma Consensus Integration: We utilize Yuma
        Consensus not just for reward distribution, but to achieve network-wide agreement
        on the validity of probabilistic agent interactions.
    - claim_field: "/documentation/status"
      source_path: "README.md"
      line_ref: "3"
      quote: "Abstract"
  unresolved:
    - "No reward/scoring function exists in the repo (docs-only)."
    - "No weight_setting call site, cadence, on-chain call, or smoothing/EMA constants present."
    - "No aggregation method, normalization, temperature, decay_rate, or burn allocation specified."
    - "No ground-truth sources or oracle/judge defined."
    - "No anti-gaming mechanisms specified despite TEE/Proof-of-Entanglement framing."
    - "No sub-competition structure or per-miner state described."
    - "netuid unknown; no concrete scoring metric, direction, or weights given."
