schema_version: "1.2.0"
instance_kind: extracted

# This corpus repo (condenses/neural-condense-subnet) contains only a thin README that
# describes a "Decentralized Text Compression Service" and links out to three separate,
# unavailable repositories: the validator (condenses/subnet-validating, which computes
# scores and sets weights), the miner (condenses/subnet-miner), and a Grafana dashboard.
# No scoring, reward, aggregation, or weight-setting code is present in this archive.
# mechanism_status is therefore unknown.
mechanism_status: unknown
mechanism_status_notes: >-
  The README in this repository documents only the product ("Advanced Lossless Text
  Compression", "Optimize your LLM costs with state-of-the-art compression technology")
  and points to external repos for validator and miner setup. The actual incentive
  mechanism (reward computation and on-chain weight setting) lives in the separate
  condenses/subnet-validating repository, which is not part of this archive. Whether
  emissions are scored or burned cannot be determined from the available source.

subnet:
  name: "Neural Condense Subnet (Decentralized Text Compression Service)"
  owner_repo: "condenses__neural-condense-subnet"
  netuid: null
  implementation_lang: none_docs_only

task:
  summary: >-
    Per the README, miners provide a decentralized lossless text compression service used
    to reduce LLM input costs ("Optimize your LLM costs with state-of-the-art compression
    technology"). The reward/scoring logic that evaluates a miner's compression output is
    not present in this repository; it is documented only by reference to an external
    validator repo (condenses/subnet-validating). The concrete submission format and the
    rewarded quantity cannot be determined from this archive.
  submission_format: [other]
  submission_format_other: >-
    Undetermined from this repo. The README describes a "text compression service" but
    contains no protocol/synapse definition; the miner/validator interface lives in
    external repositories not included in this archive.

scoring_signals: []

ground_truth_sources: []

aggregation:
  method: undocumented
  composition: other
  composition_other: "undetermined — no aggregation code in this repo"
  normalization: none
  temperature: null
  decay_rate: null
  min_weight_floor: null
  burn_allocation:
    enabled: false
    address_or_uid: null
    fraction: null
    dynamic: false
    notes: "No burn information present; no validator code in this repo."
  notes: >-
    No reward aggregation code exists in this repository. Scoring is performed by the
    external validator (condenses/subnet-validating), so the aggregation method is
    undocumented here.

sub_competitions:
  structure: none
  stages: []
  count: null
  notes: ""

per_miner_state:
  tracked: false
  state_kinds: []
  status_values: []
  reset_policy: ""
  notes: "No per-miner scoring state present in this docs-only repository."

documentation:
  status: docs_only
  source_version: null
  primary_docs: ["README.md"]
  completeness: low

provenance:
  extracted_by: "im-extract / claude-opus-4-8"
  extracted_at: "2026-06-06"
  confidence_overall: low
  evidence:
    - claim_field: "/subnet/name"
      source_path: "README.md"
      line_ref: "1-9"
      quote: "Decentralized Text Compression Service ... Advanced Lossless Text Compression ... https://condenses.ai"
      confidence: high
    - claim_field: "/subnet/implementation_lang"
      source_path: "README.md"
      line_ref: null
      quote: "Docs-only repo: only README.md and assets (image/logo). No code files present (confirmed by file listing)."
      confidence: high
    - claim_field: "/task/summary"
      source_path: "README.md"
      line_ref: "1-6"
      quote: "Advanced Lossless Text Compression ... Optimize your LLM costs with state-of-the-art compression technology"
      confidence: medium
    - claim_field: "/documentation/status"
      source_path: "README.md"
      line_ref: "17-27"
      quote: "Validator Node Setup ... [Validator Documentation](https://github.com/condenses/subnet-validating) ... Miner Node Setup ... [Miner Documentation](https://github.com/condenses/subnet-miner)"
      confidence: high
    - claim_field: "/mechanism_status"
      source_path: "README.md"
      line_ref: "17-19"
      quote: "For detailed instructions on setting up and running a validator node: [Validator Documentation](https://github.com/condenses/subnet-validating)"
      confidence: high
  unresolved:
    - "/subnet/netuid — no netuid stated anywhere in the README or any config in this repo."
    - "/task/submission_format — no protocol/synapse or miner interface in this repo; the miner/validator interface is in external repos."
    - "/scoring_signals — no reward/scoring code; all scoring lives in condenses/subnet-validating (not in archive)."
    - "/ground_truth_sources — no reference dataset/model or evaluation source documented in this repo."
    - "/aggregation/method, /aggregation/decay_rate, /aggregation/min_weight_floor — no aggregation code present; no numeric constants to cite."
    - "/weight_setting — no set_weights call site or cadence in this repo; entirely in the external validator."
    - "/anti_gaming — no anti-gaming mechanisms documented in the README."
    - "mechanism_status — cannot determine scored vs burned; validator code unavailable."
  extractor_notes: >-
    This corpus directory contains only README.md plus two image assets (logo.png,
    image.png) and lint/git config files — no source code (verified by full file listing).
    The README is a short product blurb that links to three external resources: the
    validator repo (condenses/subnet-validating), the miner repo (condenses/subnet-miner),
    and a Grafana dashboard. The incentive mechanism (reward function, aggregation, and
    on-chain weight setting) is entirely outside this archive. All code-derived fields are
    left empty and listed under unresolved; confidence_overall is low.

composition:
  shape: opaque
  overlays: []
  extern_count: 0   # provisional; coverage.py recomputes
